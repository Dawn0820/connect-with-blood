<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
	<entry key="loginMember">
		SELECT * 
		FROM MEMBER 
		WHERE USER_ID=? 
		AND   USER_PW=?
		AND   USER_STATUS='0'
			
	</entry>
	
	<entry key="enrollMember">
		INSERT INTO MEMBER(USER_NO,
						USER_ID,
						USER_PW,
						USER_NAME,
						USER_BIRTH,
						USER_EMAIL,
						USER_PHONE,
						USER_ADDRESS,
						USER_BLOODTYPE) 
						VALUES 
						(SEQ_UNO.NEXTVAL,
                   		?,
                    	?,
                    	?,
                    	?,
                    	?,
                    	?,
                    	?,
                    	?)
	</entry> <!-- 디폴트 들어가있는건 빼~ -->
	
	<entry key="loginMember">
		SELECT * 
		FROM MEMBER 
		WHERE USER_ID=? 
			AND USER_PW=? 
			<!-- status 추가해야함 -->
	</entry>
	
	<entry key="findId">
		SELECT USER_ID
		FROM MEMBER
		WHERE USER_NAME=?
		AND USER_EMAIL=?
	</entry>
	
	<entry key="findPw">
		SELECT USER_PW
		FROM MEMBER
		WHERE USER_ID=? 
		AND USER_NAME=?
		AND USER_EMAIL=? 
	</entry>
	
	<entry key="updateMem">
		UPDATE MEMBER
		SET 	USER_NAME = ?
                ,PHONE = ?
                ,EMAIL = ?
                ,ADDRESS = ?
                ,INTEREST = ?
                ,MODIFY_DATE = SYSDATE
		WHERE 	USER_ID = ?
	</entry>
	
	<entry key="selectMem">
		SELECT * 
		FROM MEMBER
		WHERE USER_ID =?
	</entry>
	
	
	
	<entry key="deleteMember">
		UPDATE MEMBER SET STATUS='WITHDRAWAL', MODIFY_DATE=SYSDATE WHERE USER_ID=? AND USER_PWD=?
	</entry>
	
	
	
		<!-- AJAX -->
	<entry key="idCheck">
		SELECT COUNT(*) COUNT 
		FROM MEMBER 
		WHERE USER_ID = ?
	</entry>
	
	

</properties>